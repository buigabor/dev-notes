{
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/client"
  ],
  "scripts": {
    "start": "yarn workspace api start",
    "build": "yarn workspace api build",
    "heroku-postbuild": "yarn workspace api heroku-postbuild"
  },
  "dependencies": {
    "jest": "^26.6.3"
  }
}
